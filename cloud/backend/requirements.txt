fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
PyMySQL>=1.1.1

# The cloud backend is expected to run the scanner ML stack during the demo.
# Keep the ML dependencies included here so `pip install -r requirements.txt`
# does not accidentally create a backend that can only run heuristics.
-r requirements-ml.txt
